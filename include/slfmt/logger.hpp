#ifndef SLFMT_LOGGER_H
#define SLFMT_LOGGER_H

#include <string>

namespace slfmt {
	inline auto test(std::string param) -> std::string {
		return param;
	}
}  // namespace slfmt

#endif // SLFMT_LOGGER_H
